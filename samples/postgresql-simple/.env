# PostgreSQL Environment Variables (Docker-compatible)

# Required: Set the PostgreSQL superuser password
POSTGRES_PASSWORD=postgres

# Optional: Create a specific database (default: postgres)
# POSTGRES_DB=myapp

# Optional: Create a specific user (uses POSTGRES_PASSWORD)
# POSTGRES_USER=myuser