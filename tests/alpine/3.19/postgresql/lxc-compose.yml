version: '1.0'
containers:
  postgresql-alpine-test:
    template: alpine-3.19
    includes:
      - postgresql
    exposed_ports: [5432]
    tests:
      internal:
        - postgresql-check:library/services/alpine/3.19/postgresql/tests/test.sh