version: '1.0'
containers:
  redis-alpine-test:
    template: alpine-3.19
    includes:
      - redis
    exposed_ports: [6379]
    tests:
      internal:
        - redis-check:library/services/alpine/3.19/redis/tests/test.sh