version: '1.0'
containers:
  redis-uminimal2404-test:
    template: ubuntu-minimal-24.04
    includes:
      - redis
    exposed_ports: [6379]
    tests:
      internal:
        - redis-check:library/services/ubuntu-minimal/24.04/redis/tests/redis.sh
