version: '1.0'
containers:
  nginx-uminimal2404-test:
    template: ubuntu-minimal-24.04
    includes:
      - nginx
    exposed_ports: [80, 443]
    tests:
      internal:
        - nginx-check:library/services/ubuntu-minimal/24.04/nginx/tests/nginx.sh
