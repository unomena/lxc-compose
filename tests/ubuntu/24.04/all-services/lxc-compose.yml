version: '1.0'
containers:
  all-services-ubuntu2404-test:
    template: ubuntu-24.04
    includes:
      - haproxy
      - nginx
      - postgresql
      - python3
      - redis
      - supervisor
    exposed_ports: [80, 443, 5432, 6379, 8080, 8404]
    post_install:
      - echo "All ubuntu-24.04 services installed"
    tests:
      internal:
