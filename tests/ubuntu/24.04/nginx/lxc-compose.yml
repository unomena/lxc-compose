version: '1.0'
containers:
  nginx-ubuntu2404-test:
    template: ubuntu-24.04
    includes:
      - nginx
    exposed_ports: [80, 443]
    tests:
      internal:
        - nginx-check:library/services/ubuntu/24.04/nginx/tests/nginx.sh
