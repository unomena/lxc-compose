version: '1.0'
containers:
  redis-ubuntu2204-test:
    template: ubuntu-22.04
    includes:
      - redis
    exposed_ports: [6379]
    tests:
      internal:
        - redis-check:library/services/ubuntu/22.04/redis/tests/redis.sh
