version: '1.0'
containers:
  all-services-ubuntu2204-test:
    template: ubuntu-22.04
    includes:
      - haproxy
      - nginx
      - postgresql
      - python3
      - redis
      - supervisor
    exposed_ports: [80, 443, 5432, 6379, 8080, 8404]
    post_install:
      - echo "All ubuntu-22.04 services installed"
    tests:
      internal:
